<div class="container">
  <div class="row">
    <% @lessons.each do |lesson| %>
      <div class="col-4">
        <div>
          <%= link_to lesson_path(lesson) do %>
          <%= render "shared/card_index", lesson: lesson %>
          <% end %>
        </div>
      </div>
     <% end %>
  </div>
</div>
